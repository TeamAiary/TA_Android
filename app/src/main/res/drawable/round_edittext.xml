<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 배경 색상 -->
    <solid android:color="@color/note_beige" />

    <!-- 둥근 정도 (radius) -->
    <corners android:radius="10dp" />

    <!-- 테두리 색과 두께 (선택사항) -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />
</shape>
